<div class="col my-2 p-0">
  <div class="card headline">
      <p class="card-header text-center">ジャンル検索</p>
      <% @genres.each do |genre| %>
       <li class="list-group-item border-0">
        <%= link_to genre.name, genre_search_path(genre_id: genre.id), method: :get, style: "color: black;" %>
      </li>
      <% end %>
    </div>
</div>